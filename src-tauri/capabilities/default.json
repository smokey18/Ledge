{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Local widget, settings and session windows",
  "windows": [
    "main",
    "settings",
    "popover-*"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
